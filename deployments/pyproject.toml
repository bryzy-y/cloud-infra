[project]
name = "deployments"
version = "0.0.1"
description = "Collection of cloud-infra service deployments"
requires-python = ">=3.14"
dependencies = ["boto3>=1.42.78"]

[dependency-groups]
dev = ["boto3-stubs[ecs]>=1.42.78"]
